# Loafly Production Python

A production-ready Python order-processing pipeline using the Python
standard library.

## Setup

Create a virtual environment:

```bash
python -m venv .venv


## Activate virtual environment
.venv\Scripts\activate